using System;

class Program
{
    // 사용자로 부터 입력을 받고
    // 사용자가 지정한 숫자부터 
    static void Main(string[] args)
    {
        int sum = 0;
        float f = 0.0f;
        string str = "";

        for (int i = 0; i < 10; i++;)
        {
            sum[i]  = 0;
        }
    }
}